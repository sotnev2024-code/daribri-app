# Настройка домена daribri.ru

## Шаг 1: Настройка DNS записей

### В панели управления доменом (у регистратора домена)

Добавьте следующие DNS записи:

**Вариант A: Если у вас есть IP адрес сервера**

```
Тип: A
Имя: @ (или daribri.ru)
Значение: [IP_АДРЕС_ВАШЕГО_СЕРВЕРА]
TTL: 3600 (или автоматически)
```

```
Тип: A
Имя: www
Значение: [IP_АДРЕС_ВАШЕГО_СЕРВЕРА]
TTL: 3600 (или автоматически)
```

**Вариант B: Если домен уже указывает на сервер через CNAME**

```
Тип: CNAME
Имя: www
Значение: daribri.ru
TTL: 3600
```

### Проверка DNS

После настройки DNS подождите 5-30 минут (распространение DNS) и проверьте:

```bash
# Проверка A записи
nslookup daribri.ru

# Проверка www поддомена
nslookup www.daribri.ru

# Должны вернуться IP адреса вашего сервера
```

---

## Шаг 2: Обновление конфигурации Nginx на сервере

### 1. Подключитесь к серверу

```bash
ssh root@your-server-ip
# или
ssh root@daribri.ru  # если SSH настроен для домена
```

### 2. Обновите конфигурацию Nginx

```bash
nano /etc/nginx/sites-available/daribri
```

**Замените содержимое на:**

```nginx
server {
    listen 80;
    server_name daribri.ru www.daribri.ru;

    # Максимальный размер загружаемых файлов
    client_max_body_size 50M;

    # Проксирование на FastAPI приложение
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Логи
    access_log /var/log/nginx/daribri_access.log;
    error_log /var/log/nginx/daribri_error.log;
}
```

**Сохраните:** `Ctrl+O`, `Enter`, `Ctrl+X`

### 3. Проверьте и перезагрузите Nginx

```bash
# Проверьте синтаксис конфигурации
nginx -t

# Если всё ОК, перезагрузите Nginx
systemctl reload nginx

# Проверьте статус
systemctl status nginx
```

---

## Шаг 3: Установка SSL сертификата (HTTPS)

### 1. Установите Certbot (если ещё не установлен)

```bash
apt update
apt install -y certbot python3-certbot-nginx
```

### 2. Получите SSL сертификат

```bash
certbot --nginx -d daribri.ru -d www.daribri.ru
```

**Во время установки:**
1. Введите email для уведомлений об истечении сертификата
2. Согласитесь с условиями: введите `A` (Agree)
3. Редирект HTTP → HTTPS: выберите `2` (Redirect) - рекомендуется

Certbot автоматически:
- Получит SSL сертификат от Let's Encrypt
- Обновит конфигурацию Nginx для HTTPS
- Настроит редирект с HTTP на HTTPS

### 3. Проверьте автоматическое обновление

```bash
# Проверьте таймер
systemctl status certbot.timer

# Тестовый запуск обновления
certbot renew --dry-run
```

---

## Шаг 4: Обновление конфигурации приложения

### 1. Обновите файл .env

```bash
cd /var/www/daribri
nano .env
```

**Обновите строку `WEBAPP_URL`:**

```env
WEBAPP_URL=https://daribri.ru
```

**Сохраните:** `Ctrl+O`, `Enter`, `Ctrl+X`

### 2. Перезапустите приложение

```bash
systemctl restart daribri
systemctl status daribri
```

---

## Шаг 5: Проверка работы

### 1. Проверка DNS

```bash
# На вашем компьютере
nslookup daribri.ru
nslookup www.daribri.ru
```

### 2. Проверка HTTP (должен редиректить на HTTPS)

```bash
curl -I http://daribri.ru
# Должен вернуть: HTTP/1.1 301 Moved Permanently
```

### 3. Проверка HTTPS

```bash
curl -I https://daribri.ru
# Должен вернуть: HTTP/2 200
```

### 4. Проверка в браузере

Откройте в браузере:
- `https://daribri.ru` - должен открыться приложение с зеленым замочком
- `http://daribri.ru` - должен автоматически перенаправить на HTTPS
- `https://www.daribri.ru` - должен работать (если настроен)

---

## Шаг 6: Обновление Telegram Bot (если используется)

Если у вас настроен Telegram Bot, обновите URL Mini App в настройках бота:

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите "Bot Settings" → "Menu Button" или "Web App"
5. Установите URL: `https://daribri.ru`

---

## Устранение проблем

### Проблема: DNS не распространился

**Решение:**
- Подождите до 24 часов (обычно 5-30 минут)
- Проверьте DNS через разные сервисы: https://dnschecker.org/
- Убедитесь, что записи добавлены правильно

### Проблема: Nginx не запускается

```bash
# Проверьте синтаксис
nginx -t

# Проверьте логи
tail -f /var/log/nginx/error.log

# Проверьте, не занят ли порт 80
netstat -tulpn | grep :80
```

### Проблема: Certbot не может получить сертификат

**Причины:**
1. DNS не указывает на сервер
2. Порт 80 закрыт в firewall
3. Nginx не работает

**Решение:**

```bash
# 1. Проверьте DNS
nslookup daribri.ru

# 2. Проверьте firewall
ufw status
ufw allow 80/tcp
ufw allow 443/tcp

# 3. Проверьте Nginx
systemctl status nginx
systemctl start nginx
```

### Проблема: Сайт открывается, но показывает старый домен

**Решение:**
1. Очистите кеш браузера: `Ctrl + Shift + R` (Windows/Linux) или `Cmd + Shift + R` (Mac)
2. Проверьте, что конфигурация Nginx обновлена:
   ```bash
   cat /etc/nginx/sites-available/daribri | grep server_name
   ```
3. Перезагрузите Nginx:
   ```bash
   systemctl reload nginx
   ```

### Проблема: Приложение не отвечает

```bash
# Проверьте статус приложения
systemctl status daribri

# Проверьте логи
journalctl -u daribri -n 50 --no-pager

# Проверьте, что порт 8000 слушается
netstat -tulpn | grep 8000

# Перезапустите приложение
systemctl restart daribri
```

---

## Чеклист после настройки

- [ ] DNS записи добавлены у регистратора домена
- [ ] DNS распространился (проверено через nslookup)
- [ ] Конфигурация Nginx обновлена для daribri.ru
- [ ] Nginx перезагружен и работает
- [ ] SSL сертификат установлен через Certbot
- [ ] HTTPS работает (зеленый замочек в браузере)
- [ ] HTTP редиректит на HTTPS
- [ ] Файл .env обновлен с новым WEBAPP_URL
- [ ] Приложение перезапущено
- [ ] Сайт открывается по https://daribri.ru
- [ ] Telegram Bot обновлен (если используется)

---

## Готово! 🎉

После выполнения всех шагов ваш проект будет доступен по адресу:
- **https://daribri.ru** - основной адрес
- **https://www.daribri.ru** - с префиксом www (если настроен)

Для обновления кода используйте:
```bash
cd /var/www/daribri && git pull origin main && systemctl restart daribri
```

---

## Дополнительно: Настройка старого домена (опционально)

Если вы хотите, чтобы старый домен `flow.plus-shop.ru` тоже работал, добавьте его в конфигурацию:

```nginx
server {
    listen 80;
    server_name daribri.ru www.daribri.ru flow.plus-shop.ru www.flow.plus-shop.ru;
    # ... остальная конфигурация ...
}
```

И обновите SSL сертификат:
```bash
certbot --nginx -d daribri.ru -d www.daribri.ru -d flow.plus-shop.ru -d www.flow.plus-shop.ru
```

