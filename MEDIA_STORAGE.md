# Хранение изображений на сервере

## Где сохраняются изображения

При размещении проекта на GitHub и запуске через Timeweb Cloud, изображения сохраняются **локально на сервере** в следующих директориях:

```
/var/www/daribri/uploads/
├── products/          # Изображения товаров
│   ├── 1/            # Товар с ID=1
│   │   ├── primary_abc123.jpg
│   │   └── media_def456.jpg
│   └── 2/
│       └── primary_ghi789.png
├── shops/             # Фото магазинов
│   ├── 1/
│   │   └── photo_xyz789.jpg
│   └── 2/
│       └── photo_abc123.png
└── shop_requests/     # Временные фото заявок на магазины
    └── request_123.jpg
```

## Важно! ⚠️

**Папка `uploads/` НЕ сохраняется в Git** (она в `.gitignore`), поэтому:

1. ✅ При первом деплое папка создастся автоматически
2. ✅ Изображения будут сохраняться на сервере
3. ⚠️ При пересоздании сервера все изображения будут потеряны
4. ⚠️ При клонировании из GitHub папка `uploads/` будет пустой

## Настройка на сервере

### 1. Создание папки uploads (автоматически)

Папка создается автоматически при первом запуске приложения, но можно создать вручную:

```bash
# На сервере
cd /var/www/daribri
mkdir -p uploads/products uploads/shops uploads/shop_requests

# Установите правильные права
chown -R www-data:www-data uploads/
chmod -R 755 uploads/
```

### 2. Проверка после деплоя

```bash
# Проверьте, что папка существует
ls -la /var/www/daribri/uploads/

# Проверьте права доступа
ls -la /var/www/daribri/uploads/products/
```

## Рекомендации для production

### Вариант 1: Локальное хранение (текущий вариант)

**Плюсы:**
- ✅ Простота настройки
- ✅ Быстрый доступ
- ✅ Не требует дополнительных сервисов

**Минусы:**
- ⚠️ Изображения теряются при пересоздании сервера
- ⚠️ Нужны регулярные бэкапы
- ⚠️ Ограничение по дисковому пространству

**Настройка бэкапов:**

```bash
# Создайте скрипт для бэкапа изображений
nano /var/www/daribri/backup_media.sh
```

Содержимое скрипта:

```bash
#!/bin/bash
BACKUP_DIR="/var/backups/daribri/media"
DATE=$(date +%Y%m%d_%H%M%S)
SOURCE_DIR="/var/www/daribri/uploads"

mkdir -p $BACKUP_DIR
tar -czf $BACKUP_DIR/uploads_$DATE.tar.gz -C $SOURCE_DIR .

# Удаляем старые бэкапы (старше 30 дней)
find $BACKUP_DIR -name "uploads_*.tar.gz" -mtime +30 -delete

echo "Бэкап создан: $BACKUP_DIR/uploads_$DATE.tar.gz"
```

Сделайте скрипт исполняемым:

```bash
chmod +x /var/www/daribri/backup_media.sh
```

Добавьте в cron для автоматических бэкапов:

```bash
# Редактируйте crontab
crontab -e

# Добавьте строку (бэкап каждый день в 3:00)
0 3 * * * /var/www/daribri/backup_media.sh
```

### Вариант 2: Облачное хранилище (рекомендуется для production)

Для production рекомендуется использовать облачное хранилище:

#### A. Yandex Object Storage (S3-совместимое)

1. Создайте бакет в Yandex Object Storage
2. Установите библиотеку:

```bash
pip install boto3
```

3. Измените `backend/app/services/media.py` для сохранения в S3

#### B. Timeweb Cloud Storage

Timeweb Cloud предоставляет S3-совместимое хранилище.

#### C. Другие варианты

- AWS S3
- Google Cloud Storage
- Azure Blob Storage

## Обновление кода без потери изображений

При обновлении кода через Git или rsync:

### ✅ Правильно (изображения сохранятся):

```bash
# Исключите папку uploads из синхронизации
rsync -avz --exclude 'uploads' --exclude '.venv' \
  --exclude '__pycache__' --exclude '*.pyc' \
  --exclude '.git' --exclude 'database/miniapp.db' \
  ./ root@your-server-ip:/var/www/daribri/
```

### ❌ Неправильно (изображения могут быть удалены):

```bash
# НЕ делайте так - это удалит папку uploads
rsync -avz --delete ./ root@your-server-ip:/var/www/daribri/
```

## Проверка работы

### 1. Проверьте загрузку изображения

```bash
# Загрузите тестовое изображение через API
curl -X POST "https://your-domain.com/api/products/1/media" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "files=@test.jpg"
```

### 2. Проверьте, что файл сохранился

```bash
# На сервере
ls -la /var/www/daribri/uploads/products/1/
```

### 3. Проверьте доступность через веб

Откройте в браузере:
```
https://your-domain.com/media/products/1/primary_abc123.jpg
```

## Миграция изображений

Если нужно перенести изображения с одного сервера на другой:

### С сервера на сервер:

```bash
# На старом сервере
cd /var/www/daribri
tar -czf uploads_backup.tar.gz uploads/

# Скопируйте на новый сервер
scp uploads_backup.tar.gz root@new-server-ip:/var/www/daribri/

# На новом сервере
cd /var/www/daribri
tar -xzf uploads_backup.tar.gz
chown -R www-data:www-data uploads/
chmod -R 755 uploads/
```

### С локального компьютера на сервер:

```bash
# Если у вас есть локальная копия uploads
rsync -avz uploads/ root@your-server-ip:/var/www/daribri/uploads/
```

## Размер хранилища

### Мониторинг использования диска:

```bash
# Проверьте размер папки uploads
du -sh /var/www/daribri/uploads/

# Проверьте размер по подпапкам
du -sh /var/www/daribri/uploads/*/

# Проверьте общее использование диска
df -h
```

### Очистка старых файлов:

Если нужно удалить неиспользуемые изображения:

```bash
# ВНИМАНИЕ: Это удалит все изображения!
# Используйте только если уверены
rm -rf /var/www/daribri/uploads/products/*
```

Лучше использовать скрипт очистки, который проверяет БД:

```python
# Создайте скрипт cleanup_unused_media.py
# Он проверит БД и удалит файлы, которых нет в базе
```

## Настройка Nginx для прямой раздачи файлов (опционально)

Для лучшей производительности можно настроить Nginx для прямой раздачи файлов:

```nginx
# В /etc/nginx/sites-available/daribri
location /media/ {
    alias /var/www/daribri/uploads/;
    expires 30d;
    add_header Cache-Control "public, immutable";
    
    # Безопасность
    location ~ \.(php|py|sh)$ {
        deny all;
    }
}
```

## Итоговая структура на сервере

```
/var/www/daribri/
├── backend/              # Код бэкенда (из Git)
├── frontend/             # Код фронтенда (из Git)
├── database/             # База данных (НЕ в Git)
│   └── miniapp.db
├── uploads/             # Изображения (НЕ в Git) ⚠️
│   ├── products/
│   ├── shops/
│   └── shop_requests/
├── venv/                 # Виртуальное окружение (НЕ в Git)
├── .env                  # Переменные окружения (НЕ в Git)
└── run_api.py            # Точка входа (из Git)
```

## Чеклист после деплоя

- [ ] Папка `uploads/` создана
- [ ] Права доступа установлены (`www-data:www-data`)
- [ ] Можно загрузить тестовое изображение
- [ ] Изображение доступно по URL
- [ ] Настроены бэкапы (рекомендуется)
- [ ] Мониторинг использования диска настроен

## Готово! 🎉

Теперь изображения будут сохраняться на сервере в папке `/var/www/daribri/uploads/`.

**Важно:** Не забывайте делать регулярные бэкапы папки `uploads/`!

