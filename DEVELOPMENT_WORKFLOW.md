# Рекомендуемый workflow для разработки и деплоя

## 🎯 Рекомендуемый подход: Git + Автоматизация

### Преимущества:
- ✅ История изменений
- ✅ Легкий откат к предыдущим версиям
- ✅ Работа в команде
- ✅ Автоматизация деплоя
- ✅ Бэкапы кода

## 📋 Настройка Git репозитория

### 1. Инициализация репозитория (если еще не сделано)

```bash
# На локальном компьютере
cd C:\Users\sotne\PycharmProjects\miniaps_flow_recurent

# Инициализируйте Git
git init

# Добавьте все файлы (кроме игнорируемых)
git add .

# Создайте первый коммит
git commit -m "Initial commit"
```

### 2. Создание репозитория на GitHub

1. Зайдите на https://github.com
2. Создайте новый репозиторий (например, `daribri-app`)
3. **НЕ добавляйте** README, .gitignore, license (они уже есть)

### 3. Подключение к GitHub

```bash
# Добавьте remote репозиторий
git remote add origin https://github.com/your-username/daribri-app.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

### 4. Настройка .gitignore (уже настроен)

Убедитесь, что в `.gitignore` есть:
- `uploads/` - изображения
- `.env` - секреты
- `database/*.db` - база данных
- `venv/` - виртуальное окружение
- `__pycache__/` - кэш Python

## 🔄 Workflow для внесения правок

### Шаг 1: Внесение изменений локально

```bash
# 1. Убедитесь, что вы на актуальной версии
git pull origin main

# 2. Создайте новую ветку для фичи (опционально, но рекомендуется)
git checkout -b feature/change-header-color

# 3. Внесите изменения в код
# ... редактируйте файлы ...

# 4. Проверьте изменения
git status
git diff

# 5. Добавьте измененные файлы
git add frontend/css/styles.css
git add frontend/index.html
# или все изменения:
git add .

# 6. Создайте коммит с понятным сообщением
git commit -m "Изменен цвет header на #dbff00 и обновлена иконка"

# 7. Отправьте на GitHub
git push origin feature/change-header-color
# или если работаете в main:
git push origin main
```

### Шаг 2: Обновление на сервере

#### Вариант A: Через Git (рекомендуется)

```bash
# На сервере
ssh root@your-server-ip
cd /var/www/daribri

# Остановите сервис
systemctl stop daribri

# Обновите код из GitHub
git pull origin main

# Установите права
chown -R www-data:www-data /var/www/daribri
chmod -R 755 /var/www/daribri

# Если изменились зависимости
source venv/bin/activate
pip install -r requirements.txt --upgrade

# Запустите сервис
systemctl start daribri

# Проверьте статус
systemctl status daribri
```

#### Вариант B: Через скрипт (автоматизация)

Создайте скрипт `update.sh` на сервере:

```bash
#!/bin/bash
# Скрипт для обновления с GitHub

set -e

APP_DIR="/var/www/daribri"
SERVICE_NAME="daribri"

cd $APP_DIR

echo "Остановка сервиса..."
systemctl stop $SERVICE_NAME

echo "Обновление кода из GitHub..."
git pull origin main

echo "Обновление зависимостей..."
source venv/bin/activate
pip install -r requirements.txt --upgrade

echo "Установка прав..."
chown -R www-data:www-data $APP_DIR
chmod -R 755 $APP_DIR

echo "Запуск сервиса..."
systemctl start $SERVICE_NAME

echo "Проверка статуса..."
sleep 2
systemctl status $SERVICE_NAME --no-pager -l

echo "✅ Обновление завершено!"
```

Сделайте исполняемым:

```bash
chmod +x /var/www/daribri/update.sh
```

Использование:

```bash
cd /var/www/daribri
sudo ./update.sh
```

## 🚀 Автоматизация деплоя

### Вариант 1: Простой скрипт на локальном компьютере

Создайте файл `deploy.sh` (или `deploy.ps1` для PowerShell):

**Для Windows PowerShell (`deploy.ps1`):**

```powershell
# Скрипт для деплоя на сервер
param(
    [string]$Server = "root@your-server-ip",
    [string]$AppDir = "/var/www/daribri"
)

Write-Host "🚀 Начинаем деплой..." -ForegroundColor Green

# 1. Коммитим изменения
Write-Host "📝 Коммитим изменения..." -ForegroundColor Yellow
git add .
$message = Read-Host "Введите сообщение коммита"
git commit -m $message
git push origin main

# 2. Подключаемся к серверу и обновляем
Write-Host "🔄 Обновляем сервер..." -ForegroundColor Yellow
ssh $Server "cd $AppDir && systemctl stop daribri && git pull origin main && chown -R www-data:www-data $AppDir && chmod -R 755 $AppDir && systemctl start daribri && systemctl status daribri --no-pager -l"

Write-Host "✅ Деплой завершен!" -ForegroundColor Green
```

**Для Git Bash (`deploy.sh`):**

```bash
#!/bin/bash
# Скрипт для деплоя на сервер

SERVER="root@your-server-ip"
APP_DIR="/var/www/daribri"

echo "🚀 Начинаем деплой..."

# 1. Коммитим изменения
echo "📝 Коммитим изменения..."
git add .
read -p "Введите сообщение коммита: " message
git commit -m "$message"
git push origin main

# 2. Обновляем на сервере
echo "🔄 Обновляем сервер..."
ssh $SERVER "cd $APP_DIR && systemctl stop daribri && git pull origin main && chown -R www-data:www-data $APP_DIR && chmod -R 755 $APP_DIR && systemctl start daribri && systemctl status daribri --no-pager -l"

echo "✅ Деплой завершен!"
```

Использование:

```bash
# Git Bash
chmod +x deploy.sh
./deploy.sh

# PowerShell
.\deploy.ps1
```

### Вариант 2: GitHub Actions (CI/CD)

Создайте файл `.github/workflows/deploy.yml`:

```yaml
name: Deploy to Server

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy to server
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          cd /var/www/daribri
          systemctl stop daribri
          git pull origin main
          source venv/bin/activate
          pip install -r requirements.txt --upgrade
          chown -R www-data:www-data /var/www/daribri
          chmod -R 755 /var/www/daribri
          systemctl start daribri
          systemctl status daribri
```

Настройте секреты в GitHub:
- Settings → Secrets → Actions
- Добавьте: `SERVER_HOST`, `SERVER_USER`, `SSH_PRIVATE_KEY`

## 📝 Рекомендуемый процесс работы

### Ежедневный workflow:

1. **Начало работы:**
   ```bash
   git pull origin main
   ```

2. **Внесение изменений:**
   - Редактируйте файлы
   - Тестируйте локально

3. **Коммит:**
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push origin main
   ```

4. **Деплой на сервер:**
   ```bash
   # Вариант 1: Вручную
   ssh root@server "cd /var/www/daribri && ./update.sh"
   
   # Вариант 2: Через скрипт
   ./deploy.sh
   
   # Вариант 3: Автоматически (если настроен GitHub Actions)
   # Просто push в main - деплой произойдет автоматически
   ```

## 🔧 Настройка на сервере (первый раз)

### 1. Клонирование репозитория

```bash
# На сервере
cd /var/www
rm -rf daribri  # Если уже есть старая версия
git clone https://github.com/your-username/daribri-app.git daribri
cd daribri
```

### 2. Настройка окружения

```bash
# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Создайте .env файл
nano .env
# Заполните необходимые переменные

# Создайте папки для медиа
mkdir -p uploads/products uploads/shops uploads/shop_requests
chown -R www-data:www-data uploads/
chmod -R 755 uploads/
```

### 3. Настройка systemd

```bash
cp systemd/daribri.service /etc/systemd/system/
systemctl daemon-reload
systemctl enable daribri
systemctl start daribri
```

### 4. Создание скрипта обновления

```bash
# Скопируйте update.sh на сервер
chmod +x update.sh
```

## 🎯 Итоговая структура workflow

```
Локальная разработка
    ↓
git commit & push
    ↓
GitHub (источник истины)
    ↓
Автоматический/ручной деплой
    ↓
Сервер (production)
```

## 📋 Чеклист для начала работы

- [ ] Git репозиторий создан и подключен к GitHub
- [ ] Код загружен на GitHub
- [ ] На сервере проект клонирован из GitHub
- [ ] Настроен `.env` на сервере
- [ ] Созданы папки `uploads/` на сервере
- [ ] Настроен systemd сервис
- [ ] Создан скрипт `update.sh` на сервере
- [ ] Создан скрипт `deploy.sh` локально (опционально)
- [ ] Настроен GitHub Actions (опционально)

## 🚨 Важные моменты

### ✅ Что ДОЛЖНО быть в Git:
- Весь код (backend, frontend)
- Конфигурационные файлы (nginx, systemd)
- Скрипты деплоя
- Документация
- `.gitignore`

### ❌ Что НЕ должно быть в Git:
- `.env` файлы с секретами
- `uploads/` (изображения)
- `database/*.db` (база данных)
- `venv/` (виртуальное окружение)
- `__pycache__/` (кэш)

### 🔐 Безопасность:

1. **Никогда не коммитьте `.env`** с реальными токенами
2. Используйте `.env.example` как шаблон
3. Храните секреты в переменных окружения на сервере
4. Используйте GitHub Secrets для CI/CD

## 💡 Дополнительные рекомендации

### Ветвление (для больших изменений):

```bash
# Создайте ветку для фичи
git checkout -b feature/new-feature

# Работайте в ветке
# ... вносите изменения ...

# Коммитьте
git add .
git commit -m "Добавлена новая фича"
git push origin feature/new-feature

# Создайте Pull Request на GitHub
# После проверки слейте в main
```

### Тегирование версий:

```bash
# После важного обновления
git tag -a v1.0.0 -m "Версия 1.0.0"
git push origin v1.0.0
```

### Откат изменений:

```bash
# Если что-то пошло не так
git log  # Найдите нужный коммит
git checkout <commit-hash>  # Вернитесь к коммиту
# или
git revert <commit-hash>  # Создайте коммит, отменяющий изменения
```

## 🎉 Готово!

Теперь у вас настроен профессиональный workflow для разработки:

1. ✅ Код в Git/GitHub
2. ✅ Легкое внесение правок
3. ✅ Простой деплой
4. ✅ История изменений
5. ✅ Возможность отката

**Рекомендуемый процесс:**
1. Вносите изменения локально
2. Коммитьте и пушите в GitHub
3. Запускайте `./deploy.sh` или обновляйте на сервере через `git pull`
4. Готово! 🚀

