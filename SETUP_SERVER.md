# Настройка сервера Timeweb Cloud - Пошаговая инструкция

## Шаг 1: Подключение к серверу

```bash
ssh root@ваш-ip-сервера
# или
ssh root@ваш-домен
```

## Шаг 2: Переход в директорию проекта

```bash
cd /var/www/daribri
# или туда, куда вы загрузили проект
ls -la  # Проверьте, что файлы на месте
```

## Шаг 3: Установка необходимых пакетов

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка Python и зависимостей
apt install -y python3 python3-pip python3-venv nginx git curl wget
```

## Шаг 4: Создание виртуального окружения и установка зависимостей

```bash
cd /var/www/daribri

# Создание виртуального окружения
python3 -m venv venv

# Активация виртуального окружения
source venv/bin/activate

# Обновление pip
pip install --upgrade pip

# Установка зависимостей
pip install -r requirements.txt
```

## Шаг 5: Создание файла .env

```bash
nano .env
```

Скопируйте и заполните следующий шаблон:

```env
# Telegram Bot (ОБЯЗАТЕЛЬНО!)
BOT_TOKEN=ваш_токен_от_BotFather
WEBAPP_URL=https://ваш-домен.com

# Server
HOST=127.0.0.1
PORT=8000

# Admin IDs (ваш Telegram ID, можно получить у @userinfobot)
ADMIN_IDS=ваш_telegram_id

# Telegram Group для заявок (если используете)
SHOP_REQUESTS_GROUP_ID=-1003694178126
SHOP_REQUESTS_TOPIC_ID=2

# YooKassa Payments (если используете)
API_KEY_YOOKASSA=ваш_токен_yookassa

# Security (ОБЯЗАТЕЛЬНО! Сгенерируйте случайную строку минимум 32 символа)
SECRET_KEY=сгенерируйте-случайную-строку-минимум-32-символа
```

**Важно:** 
- Замените `ваш_токен_от_BotFather` на реальный токен бота
- Замените `https://ваш-домен.com` на ваш домен
- Замените `ваш_telegram_id` на ваш Telegram ID

Сохраните файл: `Ctrl+O`, затем `Enter`, затем `Ctrl+X`

## Шаг 6: Инициализация базы данных

```bash
cd /var/www/daribri
source venv/bin/activate
python database/init_db.py
```

## Шаг 7: Создание необходимых директорий

```bash
mkdir -p uploads/products
mkdir -p uploads/shops
mkdir -p uploads/shop_requests
mkdir -p logs
chmod -R 755 uploads
```

## Шаг 8: Настройка прав доступа

```bash
chown -R www-data:www-data /var/www/daribri
chmod -R 755 /var/www/daribri
chmod 600 .env  # Защита .env файла
```

## Шаг 9: Настройка Nginx

```bash
# Копирование конфигурации
cp nginx/daribri.conf /etc/nginx/sites-available/daribri

# Редактирование конфигурации
nano /etc/nginx/sites-available/daribri
```

**В файле замените:**
- `your-domain.com` на ваш домен
- `www.your-domain.com` на `www.ваш-домен.com`

**Пример:**
```
server_name example.com www.example.com;
```

Сохраните файл и выйдите.

```bash
# Активация конфигурации
ln -s /etc/nginx/sites-available/daribri /etc/nginx/sites-enabled/

# Проверка конфигурации
nginx -t

# Если проверка прошла успешно, перезагрузите Nginx
systemctl reload nginx
```

## Шаг 10: Настройка Systemd сервиса

```bash
# Копирование сервиса
cp systemd/daribri.service /etc/systemd/system/

# Проверьте, что пути правильные (если нужно, отредактируйте)
nano /etc/systemd/system/daribri.service
```

Убедитесь, что в файле правильные пути:
- `WorkingDirectory=/var/www/daribri`
- `ExecStart=/var/www/daribri/venv/bin/python /var/www/daribri/run_api.py`

```bash
# Перезагрузка systemd
systemctl daemon-reload

# Включение автозапуска
systemctl enable daribri

# Запуск сервиса
systemctl start daribri

# Проверка статуса
systemctl status daribri
```

## Шаг 11: Проверка работы

```bash
# Проверка API
curl http://localhost:8000/api/health

# Просмотр логов
journalctl -u daribri -f

# Проверка статуса
systemctl status daribri
```

## Шаг 12: Настройка DNS (если еще не настроен)

В панели управления доменом настройте A-запись:
- Тип: A
- Имя: @ (или ваш поддомен)
- Значение: IP адрес вашего сервера

## Шаг 13: Настройка SSL (рекомендуется)

```bash
# Установка Certbot
apt install -y certbot python3-certbot-nginx

# Получение SSL сертификата
certbot --nginx -d ваш-домен.com -d www.ваш-домен.com

# Следуйте инструкциям на экране
```

После установки SSL Certbot автоматически обновит конфигурацию Nginx.

## Проверка финальной работы

1. **Проверьте API:**
   ```bash
   curl https://ваш-домен.com/api/health
   ```

2. **Проверьте бота:**
   - Отправьте команду `/start` боту в Telegram
   - Бот должен ответить

3. **Проверьте логи:**
   ```bash
   journalctl -u daribri -n 50
   ```

## Полезные команды

```bash
# Перезапуск приложения
systemctl restart daribri

# Просмотр логов в реальном времени
journalctl -u daribri -f

# Остановка приложения
systemctl stop daribri

# Запуск приложения
systemctl start daribri

# Проверка статуса
systemctl status daribri

# Перезагрузка Nginx
systemctl reload nginx
```

## Устранение проблем

### Если приложение не запускается:

```bash
# Проверьте логи
journalctl -u daribri -n 100

# Проверьте .env файл
cat .env

# Проверьте права доступа
ls -la /var/www/daribri

# Проверьте, что виртуальное окружение активировано в сервисе
cat /etc/systemd/system/daribri.service
```

### Если Nginx не работает:

```bash
# Проверьте конфигурацию
nginx -t

# Проверьте логи
tail -f /var/log/nginx/error.log

# Проверьте статус
systemctl status nginx
```

### Если бот не отвечает:

1. Проверьте токен в `.env` файле
2. Проверьте логи: `journalctl -u daribri -f`
3. Убедитесь, что бот запущен: `systemctl status daribri`

## Готово! 🎉

Ваше приложение должно быть доступно по адресу: `https://ваш-домен.com`

